#오븐시계
#시, 분, 필요한 시간을 받은 후, 필요한 시간을 60으로 남은 나머지, 몫을 시, 분에 더해준다.
#그리고서 분이 60보다 커지면 시에 +1, 분에 -60
#시가 24보다 커질경우 시-24를 해주는 방식이다.

H,M=map(int,input().split())
t = int(input())
H += t // 60
M += t % 60 

if M >= 60:
    H += 1
    M -= 60

if H >= 24:
    H -= 24

print(H,M)

